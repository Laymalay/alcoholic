import React from 'react';
import { withRouter } from 'react-router-dom';

const IngredientsPage = ({ history, match }) => {

  return (
   <div>IngredientsPage</div>
  );
};

export default withRouter(IngredientsPage);